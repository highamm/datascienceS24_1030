---
title: "Untitled"
format: html
---

```{r}
library(tidyverse)
library(here)
athletes_df <- read_csv(here("data/athletesdata.csv"))
```

## Section 5.7

Class Exercises

Class Exercise 1. It is not reproducible because someone else would not be able to tell which packages you are using.

Class Exercise 2.

```{r}
ggplot(data = mtcars, aes(x = hp, y = drat)) +
  geom_point(aes(colour = factor(gear))) +
  facet_wrap(~ cyl) +
  geom_smooth()
```

Class Exercise 3.

```{r}
library(palmerpenguins)
penguins |> mutate(flipper_ratio = flipper_length_mm / body_mass_g) |>
  group_by(species, island) |>
  summarise(mean_flipper = mean(flipper_ratio, na.rm = TRUE)) |>
  arrange(mean_flipper) |>
  pivot_wider(names_from = c("species"), values_from = "mean_flipper")
```

Class Exercise 4.

```{r}
library(here)
videogame_df <- read_csv(here("data/videogame_clean.csv"))
videogame_df |>
  summarise(mean_pt = mean(median_playtime, na.rm = TRUE),
            median_pt = median(median_playtime, na.rm = TRUE))

videogame_df |> filter(median_playtime < 1000) |>
  summarise(mean_pt = mean(median_playtime, na.rm = TRUE),
            median_pt = median(median_playtime, na.rm = TRUE))
```

Class Exercise 5.

```{r}
library(tidyverse)
toy_df <- tibble(term = c("F19", "S20", "S20", "F20", "S21"))
toy_df
## how can i create a term variable and a year variable?
```

Your Turns

Your Turn 3.

```{r}
penguins |> mutate(flipper_ratio = flipper_length_mm / body_mass_g) |>
  relocate(flipper_ratio) |>
  filter(flipper_ratio > median(flipper_ratio, na.rm = TRUE)) |>
  group_by(species) |>
  summarise(count_var = n())
```

Your Turn 4.

```{r}
### NOT A REPREX: people do not have access to athletesdata
athletes_df <- read_csv(here("data/athletesdata.csv"))
athletes_df
## how can I keep the maximum endorsements for each Sport?

## REPREX
library(tidyverse)
sport_toy <- tibble(Sport = c("Football", "Basketball", "Football", "Football",
                 "Basketball"),
       endorsements = c(55555, 44444, 6674543, 42312, 5253))
## how can I keep the maximum endorsements for each Sport?

```



